{
  "name": "codesensei",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev",
    "start": "cd client && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 